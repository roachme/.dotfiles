[core]
	editor = nvim

[user]
    name = Amonov Hurshed
    email = qcockroach6@gmail.com

[init]
	defaultBranch = master

[credential]
    username = roachme
    helper = "!f() { test \"$1\" = get && echo \"password=$(pass ootheca/gittoken)\"; }; f"

# REuse REcorded REsolution (to automatically resolve conflicts)
[rerere]
	enabled = true

#[includeIf "gitdir:~/work/"]
#	path = ~/work/.gitconfig
